Agent Evals

This folder contains evaluation templates and tests for the AI-Application-Engineer agent. Use these to validate prompt outputs, input/output contracts, and integration flows.

Guidance:
- Keep tests fast and deterministic.
- Mock external model/network calls where possible.
- Use `pytest` for execution: `pytest evals/`.
