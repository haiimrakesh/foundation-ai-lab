This folder holds supporting assets for the AI Application Engineer agent.

- `example_data/` — small sample files used in demos (kept intentionally tiny).
- `notebooks/` — optional Jupyter notebooks demonstrating local runs.
- `schema/` — JSON schemas for inputs and outputs.

Keep assets minimal and reference them from `prompt.md` when used in examples.
